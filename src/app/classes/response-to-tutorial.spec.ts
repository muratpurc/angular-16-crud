import { ResponseToTutorial } from './response-to-tutorial';

describe('ResponseToTutorial', () => {
  it('should create an instance', () => {
    expect(new ResponseToTutorial()).toBeTruthy();
  });
});
